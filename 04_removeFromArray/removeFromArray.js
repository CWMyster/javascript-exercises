const removeFromArray = function(myArray, itemsRemoved) {
    
    //let itemArray = [].push(itemsRemoved);
    //let countItems = itemArray.length;
    console.log(countItems);
    //myArray.forEach(element => console.log(element))
    itemArray = myArray.splice(itemsRemoved - 1, countItems);
    itemArray = 

    return myArray;

};

module.exports = removeFromArray;
